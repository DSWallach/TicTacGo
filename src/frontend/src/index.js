import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Import any CSS files if needed
import App from './App'; // Import your main App component

ReactDOM.render(
    <React.StrictMode>
    <App />
    </React.StrictMode>,
    document.getElementById('root')
);
